/**
 * @format
 */

import {AppRegistry} from 'react-native';
import VirtualizedListF from './components/VirtualizedList';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => VirtualizedListF);
